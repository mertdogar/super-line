# API reference

The API reference is generated from the source TSDoc and wired in next. It will cover every export, option, and type across the five packages:

- `@super-line/core` — `defineContract`, validation, `SocketError`, `Serializer` / `Adapter` interfaces
- `@super-line/server` — `createSocketServer`, `implement`, rooms, topics, `forRole`, `emitServer` / `onServer`
- `@super-line/client` — `createClient`, typed calls, `on` / `subscribe`
- `@super-line/adapter-redis` — `createRedisAdapter`
- `@super-line/react` — `createSocketReact`
