# Copy to .env and fill in. The board works without these — only the AI agent needs them.
AI_GATEWAY_API_KEY=
# Any cheap tool-capable model on the AI Gateway. Plain "provider/model" string.
MODEL=anthropic/claude-haiku-4.5
